spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aeps
    username: aeps
    password: aeps
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    mail:
      host: smtp.gmail.com
      port: 587
      username: autech.qualityassurance@gmail.com
      password: oeskdkkcjshmkgxm
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
              required: true